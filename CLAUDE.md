# CLAUDE.md

## 上下文加载

读取本文件后，必须立即读取 docs/ai-coding-tools/info.md，获取项目技术栈、目录结构、常用命令等信息。

## 项目概述

ai-coding-tools 是一个收集和开发 AI 辅助编码工具的仓库，包含 Claude Code 状态栏配置、Skill 等实用工具。项目处于开发初期，功能在逐步添加中。

## AI 行为偏好

### 回复风格
- 主动展示所有细节和背景，不需要用户追问就展开完整说明
- 回复使用中文，代码标识符保持英文原样

### 代码风格
- 优先编辑现有文件而非创建新文件
- 代码注释使用中文
- 不过度设计，不提前引入不需要的模块或依赖（YAGNI）
- 不加多余的错误处理或向后兼容 hack

### 协作偏好
- **讨论优先**：方案先讨论，未经确认不执行
- **不擅自行动**：不擅自删除/引入/优化
- **主动建议**：可主动建议，经同意后推进
- **上下文连贯**：基于已有文档和结论工作，不脱离基础重来

## 文档规范

**更新策略：** 执行操作后主动思考是否需更新文档，如需要则按写入规范执行

**写入规范：**
- 单次写入超过 200 行时，分段完成（每次 100-200 行）
- 文档内容过大时，先按主题或模块拆分为分文档，依次写入
- 拆分后的文档通过索引文档组织，避免上下文丢失

**命名规范：**
- 文件名、目录名使用小写字母，单词间用连字符 `-` 连接
- 不要使用空格
